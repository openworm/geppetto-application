import * as ST from './selectors'
import { click, wait4selector } from './utils';

export const openProject = async (page, expectedCameraPosition) => {
};

export const deleteProject = async (page, url, id) => {
};

export const reloadProject = async (page, expectedCameraPosition) => {
};

export const createExperimentTest = async (page, expectedCameraPosition) => {
};

export const cloneExperimentTest = async (page, expectedCameraPosition) => {
};

export const deleteExperimentTest = async (page, expectedCameraPosition) => {
};

export const saveExperimentTest = async (page, expectedCameraPosition) => {
};

export const saveProjectTest = async (page, expectedCameraPosition) => {
};